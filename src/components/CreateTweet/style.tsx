import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  gap: 12px;
  padding: 8px 16px;
  border-bottom: 1px solid #eee;
  border-top: none;

  & > img {
    border-radius: 50%;
  }
`;
